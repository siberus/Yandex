// Unsolved

// #include <iostream>

// using namespace std;


// int main() {
//     int width, height, numDiplomas;
//     cin >> width >> height >> numDiplomas;

    // int mid, maxNumDiplomas, low = (width > height)?(width):(height), high = low * numDiplomas;
//     while (high - low > 1) {
//         mid = (high + low) / 2;
//         maxNumDiplomas = (mid / width) * (mid / height);
//         if (maxNumDiplomas < numDiplomas) {
//             low = mid;
//         } else {
//             high = mid;
//         }
//     }
//     cout << high << endl;

//     return 0;
// }